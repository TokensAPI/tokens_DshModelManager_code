# Commit conventions

- Use Conventional Commits (e.g. `feat(config): ...`, `fix(plugin): ...`).
- Never append a `Co-Authored-By:` (or any AI-tooling) trailer to commit messages: GitHub counts such trailers as separate contributors and these were all stripped from history — do not reintroduce them.
